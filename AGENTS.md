# sawyerwelden.com

Before changing or creating visible UI in this repository, read `STYLE.md` and
use it as the default design language unless the user explicitly requests a
different direction.

Preserve the repository's static GitHub Pages structure. Reuse the canonical
mana symbols documented in `STYLE.md` rather than drawing replacements.
